# 課題 No.03｜映画館のページ制作

## 基本情報
| 項目 | 内容 |
|------|------|
| 課題番号 | 03 |
| 主題 | 映画館のページ制作 |
| 納期 | 6月3日（水）当日中 |
| 作業時間 | 5月27日（水）1時間目 |

---

## 提出仕様
- **フォルダ名**：`03_it11x_99_name`
- **提出形式**：Zipに圧縮して提出

---

## ページ構成（掲載事項）
1. `<header>` エリア
2. `<main>` エリア（内部に `<section>`）
3. `<footer>` エリア ※ `copyright wf11` と表記
4. 映画を **2本** 紹介（内容は自由）
5. 各映画の **オフィシャルサイトへのリンク**
6. **ページ上部へのページ内リンク**
7. 各映画の **上映スケジュール**（テーブルで掲載）

---

## 必須タグ一覧
| # | タグ | 備考 |
|---|------|------|
| 1 | `<h○>` | h1〜レベルを考慮して使用 |
| 2 | `<p>` | 文章 |
| 3 | `<br>` | 任意（使わなくてもOK） |
| 4 | `<img>` | 適宜タグで構造化 |
| 5 | `<video>` / `<iframe>` | YouTubeでOK |
| 6 | `<header>` | ヘッダーエリア |
| 7 | `<main>` | メインエリア |
| 8 | `<footer>` | フッターエリア |
| 9 | `<section>` | 各情報を構造化 |

---

## 今回追加する技術（前回からの追加）
1. `<a href="">` リンクタグ
2. `<table>` / `<tr>` / `<th>` / `<td>` テーブルタグ
3. `<video>` や `<iframe>` による YouTube・動画埋め込み

---

## メモ
- 参考画像 `finish_image.png` は**最低限の仕様**。情報・機能の追加はOK
- 上記9タグは**全て使用必須**（`<br>`のみ任意）
- CSS使用・他タグ追加もOK